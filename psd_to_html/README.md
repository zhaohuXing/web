## 认识认识 CSS 标签

#### position

position 用于元素的定位，定位的种类分为如下几种：
- 相对定位（relative），相对于原始位置
- 绝对定位 （absolute），相对于整个页面来定位，可以放置到页面的任何位置

#### left 

#### 盒子模型

原来每一个元素的本身是有内边距(padding)，外边距(margin)之说的。就好像一个元素先是套上了一个壳子(内边距)，会遮挡其他元素，又套了一个透明的壳子，不会遮挡其他元素。二者中间的就是边框(border)。
